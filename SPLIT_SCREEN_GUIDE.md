# 分屏功能使用指南

本项目现在支持两种分屏方案，可以根据不同场景选择使用。

## 方案 1: tmux 分屏 (推荐用于单终端多窗格)

### 特点
- ✅ 性能最优，只使用一个 iframe
- ✅ tmux 原生支持，稳定可靠
- ✅ 分屏状态持久化在 tmux 会话中
- ✅ 可以使用 tmux 快捷键操作

### 使用方法

在浮动命令面板的标题栏中，有四个 tmux 分屏控制按钮：

1. **左右分屏** (Columns 图标)
   - 创建垂直分割，终端左右排列
   - 执行命令: `tmux split-window -h`

2. **上下分屏** (Rows 图标)
   - 创建水平分割，终端上下排列
   - 执行命令: `tmux split-window -v`

3. **最大化/还原** (Maximize 图标)
   - 切换当前窗格的最大化状态
   - 执行命令: `tmux resize-pane -Z`

4. **关闭窗格** (X 图标)
   - 关闭当前活动的窗格
   - 执行命令: `tmux kill-pane`

### tmux 快捷键参考

如果你熟悉 tmux，也可以直接使用快捷键：

- `Ctrl+b %` - 左右分屏
- `Ctrl+b "` - 上下分屏
- `Ctrl+b 方向键` - 切换窗格
- `Ctrl+b z` - 最大化/还原当前窗格
- `Ctrl+b x` - 关闭当前窗格

---

## 方案 2: iframe 多终端模式 (推荐用于多 bot 管理)

### 特点
- ✅ 可视化界面，直观易用
- ✅ 支持连接不同的 bot/终端
- ✅ 可拖拽调整分屏比例
- ✅ 支持多种布局模式

### 使用方法

#### 1. 进入多终端模式

点击浮动面板标题栏的 **Layout** 图标（紫色按钮），或者在最小化状态下点击右上角的 **Multi-Terminal** 按钮。

#### 2. 添加终端

1. 点击右上角的绿色 **+** 按钮
2. 输入 bot 名称（例如：`cicy_master_xk_bot`）
3. 按 Enter 或点击 "Add Terminal" 按钮

#### 3. 切换布局模式

添加多个终端后，可以在右上角选择布局：

- **Columns** (左右分屏) - 适合 2 个终端
- **Rows** (上下分屏) - 适合 2 个终端
- **Grid** (网格布局) - 适合 3-4 个终端

#### 4. 调整分屏比例

在两个终端之间的分隔线上：
- 鼠标悬停会显示拖拽手柄
- 按住鼠标左键拖动即可调整比例

#### 5. 关闭终端

每个终端右上角有一个红色的 **X** 按钮，点击即可关闭该终端。

#### 6. 退出多终端模式

点击右上角的红色 **X** 按钮，返回单终端模式。

---

## 使用场景建议

### 使用 tmux 分屏的场景：
- 在同一个 bot 中需要多个窗格
- 需要快速分屏和切换
- 希望分屏状态持久化
- 熟悉 tmux 操作

### 使用 iframe 多终端模式的场景：
- 需要同时查看多个不同的 bot
- 需要可视化的布局控制
- 需要独立控制每个终端
- 不熟悉 tmux 命令

---

## 技术实现

### tmux 分屏
- 通过 `/api/tmux` 接口发送 tmux 命令
- 直接操作 tmux 会话的窗格
- 所有操作在服务端的 tmux 会话中执行

### iframe 多终端
- 使用 React 组件管理多个 iframe
- 每个 iframe 连接到不同的 ttyd 实例
- 使用 `SplitPaneLayout` 组件实现可拖拽分屏
- 支持水平、垂直、网格三种布局模式

---

## 常见问题

**Q: 两种分屏可以同时使用吗？**
A: 可以！你可以在 iframe 多终端模式中，每个终端内部再使用 tmux 分屏。

**Q: tmux 分屏后如何切换窗格？**
A: 使用 `Ctrl+b` 然后按方向键，或者直接用鼠标点击终端中的不同区域。

**Q: iframe 多终端模式下，命令面板控制哪个终端？**
A: 当前版本的命令面板只控制初始的 bot。未来版本会支持选择目标终端。

**Q: 如何保存我的分屏布局？**
A: tmux 分屏会自动保存在 tmux 会话中。iframe 多终端模式的布局目前不会持久化，刷新页面后需要重新设置。

---

## 快捷操作提示

1. **快速 tmux 左右分屏**: 点击 Columns 图标
2. **快速添加新终端**: 多终端模式下点击绿色 + 按钮
3. **快速切换布局**: 多终端模式下使用布局切换按钮
4. **快速返回单终端**: 点击红色 X 退出多终端模式
